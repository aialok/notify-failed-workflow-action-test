const sendSlackNotification = async (
  slack_webhook,
  workflow_name,
  workflow_url,
  workflow_conclusion,
  failed_job_name,
  failed_job_url
) => {
  console.log('Sending slack notification');
};
